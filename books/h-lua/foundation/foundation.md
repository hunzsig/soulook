基础库

---

```
基础库包含了一些常用的方法,如下：
   
    runtime：全局变量，库运行时产生的数据源
    color：默认的颜色，方便使用
    json：encode/decode
    MD5：用于生成MD5（内置到string模块）
    debug：主要用于打印调用栈
    string：字符串库，编解码md5等都在这
    table：表库，对table的操作
    math：计算库，跟数字有关的都在这
    少量的BJ函数：bj.开头调用，与cj相似（可选择不使用）

```



